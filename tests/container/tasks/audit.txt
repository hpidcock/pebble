# begin test_tasks_abs_time
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/etc/localtime, O_RDONLY)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end test_tasks_abs_time
# begin test_tasks_format_json
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/etc/localtime, O_RDONLY)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end test_tasks_format_json
# begin test_tasks_shows_tasks
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/etc/localtime, O_RDONLY)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end test_tasks_shows_tasks
# begin test_tasks_unknown_change
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
socket(AF_UNIX, SOCK_STREAM)
# end test_tasks_unknown_change
