# begin exec_env_var
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end exec_env_var
# begin exec_exit_code
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
socket(AF_UNIX, SOCK_STREAM)
# end exec_exit_code
# begin exec_runs_command
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end exec_runs_command
# begin exec_timeout
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
socket(AF_UNIX, SOCK_STREAM)
# end exec_timeout
# begin exec_user_group
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end exec_user_group
# begin exec_with_context
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end exec_with_context
# begin exec_working_dir
connect(AF_UNIX, $PEBBLE_DIR/.pebble.socket)
openat(/root/.config/pebble/cli.json, O_RDONLY)
socket(AF_UNIX, SOCK_STREAM)
# end exec_working_dir
